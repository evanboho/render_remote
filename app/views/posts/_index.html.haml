%h1 Listing posts

%table
  %tr
    %th Title
    %th Body
    %th
    %th
    %th  
  %tr

  - @posts.each do |post|
    %tr
      %td= post.title
      %td= post.body
      %td= link_to 'Show', post, :remote => true
      %td= link_to 'Edit', edit_post_path(post), :remote => true
      %td= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' }
      
%br

= link_to 'New Post', new_post_path, :remote => true
